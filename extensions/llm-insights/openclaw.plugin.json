{
  "id": "llm-insights",
  "name": "LLM Insights",
  "description": "Summarizes available models, local token/cost usage, provider quota windows, and assistant-turn latency from transcripts.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "defaultDays": {
        "type": "number",
        "minimum": 1,
        "maximum": 365,
        "description": "Default lookback window when the tool omits days/start/end."
      },
      "defaultLimit": {
        "type": "number",
        "minimum": 1,
        "maximum": 500,
        "description": "Default max sessions to scan for session aggregates."
      }
    }
  }
}
